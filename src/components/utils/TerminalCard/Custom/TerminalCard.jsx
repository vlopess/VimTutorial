import  "../TerminalCard.css";

export const TerminalCard = ({children}) => {
    return (
        <>
            <div className="card2">
                {children}
            </div>
        </>
    );
}